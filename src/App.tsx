import "./App.css";

import { Button } from "@material-ui/core";
import React from "react";

const App: React.FC = () => {
    return (
        <Button variant="contained" color="primary">
            RAWR
        </Button>
    );
};

export default App;
